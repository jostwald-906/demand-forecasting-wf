services:
  app:
    build:
      context: .
      dockerfile: .devcontainer/Dockerfile
    volumes:
      - .:/workspace:z
    ports:
      - "5173:5173"
    environment:
      - NODE_ENV=development
    command: sleep infinity
    working_dir: /workspace
